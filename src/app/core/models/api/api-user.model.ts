export interface SignedUser {
    id: number;
    email: string;
    firstName: string;
    lastName: string;
    token: string;
}

export interface User {
    id: number;
    email: string;
    firstName: string;
    lastName: string;
    token: string;
}
